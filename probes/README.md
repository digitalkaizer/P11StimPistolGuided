# Diagnostic probe archive

These are research probes, not released gameplay mods.

- v0.1-v0.4 are retained as historical source but their live-memory address conclusions were invalidated by self-match contamination.
- v0.5 introduced full self-allocation exclusion.
- v0.5-v0.10 provide useful negative/discriminating evidence.
- v0.11 is kept under `unsafe/` rather than here because its MEM_IMAGE fallback was associated with white flickering and CTDs.

Read `docs/PROBE_HISTORY.md` before reusing any probe logic.
